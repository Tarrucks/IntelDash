{
  "name": "aperture-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Aperture OSINT platform — repo-root package.json exists so Vercel's Corepack picks the right pnpm before cd-ing into ./frontend.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:frontend": "cd frontend && pnpm install --no-frozen-lockfile",
    "build:frontend": "cd frontend && pnpm build"
  }
}
